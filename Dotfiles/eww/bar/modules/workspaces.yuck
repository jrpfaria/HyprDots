(deflisten workspaces :initial "" `./bar/scripts/workspaces.sh`)

(defwidget workspaces []
	(box
		:class "workspace-container"
		:hexpand false
		:orientation "h"
		(literal
			:content "${workspaces}"
		)
	)
)
